<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SysLogDao">

    <select id="findSyslogEntities" resultType="SysLog" parameterType="Long">
        SELECT *
        FROM SystemEvents
        WHERE ID >= #{afterId}
                AND Priority = 5
                AND Facility = 5
    </select>

</mapper>